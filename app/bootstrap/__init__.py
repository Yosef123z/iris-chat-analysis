"""
IRIS Voice Agent — Bootstrap Package
======================================
Application lifecycle management: startup wiring, cache pre-warming,
and graceful shutdown orchestration.

Modules:
    lifespan  — FastAPI lifespan context manager (startup / shutdown)
    prewarm   — Semantic-cache pre-warming with common queries
"""
